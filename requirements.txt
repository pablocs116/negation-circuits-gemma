# Minimal deps for the Neuronpedia-API workflow.
# No torch / transformers / circuit-tracer — graphs are generated server-side.
# See CLAUDE.md ("DO NOT DO") for why the heavy local stack is intentionally excluded.
neuronpedia==1.2.0
python-dotenv>=1.0
requests>=2.28
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
jupyter>=1.0
notebook>=7.0
